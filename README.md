
  # Add photos as instructed

  This is a code bundle for Add photos as instructed. The original project is available at https://www.figma.com/design/BrTQA5CBSXn6eXILMyYrRn/Add-photos-as-instructed.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  